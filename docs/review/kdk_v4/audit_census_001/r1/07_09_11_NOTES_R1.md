# r1 — Corrections to artifacts 07, 09, 11

**R1 HEAD 323e36a.** Small, targeted corrections; the originals stand except as noted.

## Artifact 07 (Data & Evidence) — one refinement
- "There is no artifact in which an owner or reviewer records acceptance" → narrow to
  **formal owner-signed acceptance**. A `TECHNICAL_REVIEW_VERDICT` (WP-L1-02, accepted/closed in
  the review narrative) and a `HANDOVER_COMPLETE` (Stage 2 package) DO exist. See r1/01 taxonomy.
- Everything else in 07 stands: 15 EvidenceIds re-hash 15/15, longest live window 160 s,
  D3 = CONTRACT_ONLY/BLOCKED, dataset qualification 4/6 unmet. Re-confirmed at HEAD.

## Artifact 09 (Gaps & Risks) — corrections
- **GAP-01** "the domain is specified, barely built" → **RETRACTED**. Correct: specified, built at
  module granularity (23/25 R3E), **untraced** at requirement granularity (591 UNMAPPED_IN_02B),
  uncalibrated (0/134). See r1/03.
- **GAP-07** (StructuralAnalysisSnapshot "broken by rename") → restate: §41 is a REVIEW-REQUIRED
  **proposed** contract not yet approved; `GcaeRuntimeSnapshot` covers the domains but is not that
  contract. It is `UNMAPPED / CONTRACT_NOT_APPROVED`, not a broken rename. See r1/04.
- **RISK-01** "383 NotCalibrated guards" → 383 textual / 84 declarations / 10 control-flow / 45
  comments across 68 files. See r1/06. The risk (green build reads as "works") stands.
- All other GAP/RISK/GOV rows stand. **GOV-Q1** → SOURCE_AVAILABILITY_CONFLICT (see R1 review).

## Artifact 11 (Recovery Plan) — one correction
- "next smallest WP = WP-D2-PROVE (wire RecoveryScanner + one live session)" → **gated**. That is
  `KDK-D2-001` and requires owner **Q1 (ADRs) + Q9 (recorder path)** first. Absent authority, the
  executed next step is **`KDK-D2-CONTRACT-FIT-001`** (done, analysis only — no wiring changed).
- Critical path, parallel work, dataset dependency, and "must not begin yet" lists all stand.
